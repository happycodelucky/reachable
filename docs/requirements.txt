# Pinned exactly so local `mkdocs build` and CI build the same site.
# Bump these in lockstep; web-search latest stable before bumping.
mkdocs==1.6.1
mkdocs-material==9.7.6
mkdocs-macros-plugin==1.3.7
pymdown-extensions==10.21.2
